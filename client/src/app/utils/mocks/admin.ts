import { Dictionary } from '@common/dictionary';
import { VirtualPlayerName } from '@common/player';
import { VirtualPlayerType } from '@common/virtualPlayer';

export const stubVirtualPlayerName: VirtualPlayerName = { name: 'name', type: VirtualPlayerType.debutant, isReadonly: false };
export const stubDictionnary: Dictionary = { title: 'title', description: 'description', words: ['words'] };
